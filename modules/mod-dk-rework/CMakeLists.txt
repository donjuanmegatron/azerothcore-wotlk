# mod-dk-rework CMakeLists.txt
# AzerothCore's parent build system automatically discovers and compiles
# all .cpp files found under this module's src/ directory.
# No additional configuration is needed here.
