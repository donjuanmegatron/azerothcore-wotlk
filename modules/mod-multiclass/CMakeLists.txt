# mod-multiclass CMakeLists.txt
# When building with MODULES="static", AzerothCore's parent build system
# automatically discovers and compiles all .cpp files in this directory.
# No additional configuration is needed here.
